<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PoCkDetection" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange_custom</item>
        <item name="colorPrimaryVariant">@color/orange_custom_dark</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/light_green600</item>
        <item name="colorSecondaryVariant">@color/light_green600_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonThemeConfirmation">
        <item name="android:background">?colorPrimary</item>
        <item name="android:drawablePadding">12dp</item>
        <item name="android:padding">8dp</item>
    </style>

    <style name="ButtonThemeChoice">
        <item name="android:minHeight">48dp</item>
        <item name="drawableTint">@color/disabled_text</item>
        <item name="android:textColor">@color/disabled_text</item>
    </style>
</resources>